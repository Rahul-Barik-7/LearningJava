📘 TYPE CASTING IN JAVA (DETAILED NOTES)
🔹 PART 1: PRIMITIVE TYPE CASTING
✅ Definition

Primitive type casting in Java means converting one primitive data type into another.
There are 8 primitive data types:

Integral → byte, short, int, long

Floating-point → float, double

Character → char

Boolean → boolean (⚠ not castable)

Primitive casting is of two types:

Widening (Upcasting) – smaller → larger (automatic)

Narrowing (Downcasting) – larger → smaller (manual, may lose data)

1) Widening (Upcasting)

➡ Smaller data type is automatically converted into larger data type.
➡ No data loss.

Conversion Hierarchy:
byte → short → int → long → float → double

Example 1: byte → short
public class ByteToShort {
    public static void main(String[] args) {
        byte b = 10;
        short s = b;   // automatic widening
        System.out.println("Byte value: " + b);
        System.out.println("Short value: " + s);
    }
}

Example 2: short → int
public class ShortToInt {
    public static void main(String[] args) {
        short s = 200;
        int i = s;   // widening
        System.out.println("Short value: " + s);
        System.out.println("Int value: " + i);
    }
}

Example 3: int → long
public class IntToLong {
    public static void main(String[] args) {
        int i = 50000;
        long l = i;   // widening
        System.out.println("Int value: " + i);
        System.out.println("Long value: " + l);
    }
}

Example 4: long → float
public class LongToFloat {
    public static void main(String[] args) {
        long l = 100000L;
        float f = l;   // widening
        System.out.println("Long value: " + l);
        System.out.println("Float value: " + f);
    }
}

Example 5: float → double
public class FloatToDouble {
    public static void main(String[] args) {
        float f = 12.34f;
        double d = f;   // widening
        System.out.println("Float value: " + f);
        System.out.println("Double value: " + d);
    }
}

2) Narrowing (Downcasting)

➡ Larger data type is manually converted into smaller data type.
➡ Done using type casting operator (type).
➡ Data loss may occur.

Conversion Hierarchy (reverse):
double → float → long → int → short → byte

Example 1: double → float
public class DoubleToFloat {
    public static void main(String[] args) {
        double d = 99.99;
        float f = (float) d;   // explicit narrowing
        System.out.println("Double value: " + d);
        System.out.println("Float value: " + f);
    }
}

Example 2: float → long
public class FloatToLong {
    public static void main(String[] args) {
        float f = 123.45f;
        long l = (long) f;   // explicit narrowing
        System.out.println("Float value: " + f);
        System.out.println("Long value: " + l);
    }
}

Example 3: long → int
public class LongToInt {
    public static void main(String[] args) {
        long l = 100000L;
        int i = (int) l;   // explicit narrowing
        System.out.println("Long value: " + l);
        System.out.println("Int value: " + i);
    }
}

Example 4: int → short
public class IntToShort {
    public static void main(String[] args) {
        int i = 32770;
        short s = (short) i;   // data loss
        System.out.println("Int value: " + i);
        System.out.println("Short value: " + s);
    }
}

Example 5: short → byte
public class ShortToByte {
    public static void main(String[] args) {
        short s = 130;
        byte b = (byte) s;   // data loss
        System.out.println("Short value: " + s);
        System.out.println("Byte value: " + b);
    }
}

❓ Questions – Primitive Casting

Q1. What is widening casting?
➡ Smaller → larger type, automatic, no data loss.

Q2. What is narrowing casting?
➡ Larger → smaller type, explicit, may lose data.

Q3. Why boolean is not castable?
➡ Because true/false cannot be expressed as a number.

🔹 PART 2: NON-PRIMITIVE (OBJECT) TYPE CASTING
✅ Definition

Non-primitive type casting means converting one object type into another within inheritance hierarchy.

1) Upcasting (Child → Parent)

Implicit / Automatic.

Always safe.

Used to generalize objects.

class Animal {
    void sound() {
        System.out.println("Animal makes sound");
    }
}

class Dog extends Animal {
    void bark() {
        System.out.println("Dog barks");
    }
}

public class UpcastingExample {
    public static void main(String[] args) {
        Animal a = new Dog();   // Upcasting
        a.sound();              // Accessible
        // a.bark(); ❌ Not allowed
    }
}


Output:

Animal makes sound

2) Downcasting (Parent → Child)

Explicit (manual).

Needed to access child-specific methods.

May cause ClassCastException if not valid.

class Animal {
    void sound() {
        System.out.println("Animal makes sound");
    }
}

class Dog extends Animal {
    void bark() {
        System.out.println("Dog barks");
    }
}

public class DowncastingExample {
    public static void main(String[] args) {
        Animal a = new Dog();        // Upcasting
        Dog d = (Dog) a;             // Downcasting
        d.sound();
        d.bark();
    }
}


Output:

Animal makes sound
Dog barks

3) Invalid Downcasting
class A {}
class B extends A {}
class C extends A {}

public class InvalidCasting {
    public static void main(String[] args) {
        A obj = new B();
        C c = (C) obj;   // ❌ Runtime error
    }
}


Output:

Exception in thread "main" java.lang.ClassCastException

❓ Questions – Object Casting

Q1. What is upcasting in objects?
➡ Assigning child object to parent reference.

Q2. What is downcasting in objects?
➡ Assigning parent reference back to child reference.

Q3. Why do we use downcasting?
➡ To access child-specific methods after upcasting.

✅ Quick Summary

Primitive Casting:

Widening → Automatic (safe)

Narrowing → Manual (may lose data)

Examples: byte → short → int → long → float → double

Non-Primitive Casting:

Upcasting → Child → Parent (automatic, safe)

Downcasting → Parent → Child (manual, may throw exception)